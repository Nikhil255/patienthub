# DataSource Configuration
spring.datasource.url=jdbc:h2:mem:testdb
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update

# Cache Configuration
spring.cache.cache-names=patients

spring.profiles.active=dev
